[build]
builder = "nixpacks"

[deploy]
startCommand = "uvicorn main:app --host 0.0.0.0 --port $PORT"
restartPolicyType = "ON_FAILURE"

# Variables de entorno que Railway inyectará automáticamente
[environments.production.variables]
DEEPSEEK_API_KEY = "sk-af05331f5cac4f078d67957b46423e16"
